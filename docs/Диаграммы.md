# UML-диаграммы системы складского учёта

**Разработчик:** Чуляков Семён Игоревич  
**Группа:** 11/2-РПО-24/2  
**Дата:** 13.12.2025

---

## 1. Диаграмма вариантов использования (Use Case Diagram)

### 1.1. Основные актёры системы

```mermaid
flowchart TD
    Actors[Актёры системы] --> A1[👤 Кладовщик]
    Actors --> A2[👔 Менеджер склада]
    Actors --> A3[🛠️ Администратор]
    Actors --> A4[🔍 Аудитор]
```

### 1.2. Варианты использования по ролям

```mermaid
graph TD
    subgraph "Кладовщик 👤"
        K1[Приёмка товара]
        K2[Отпуск товара]
        K3[Перемещение товаров]
        K4[Проведение инвентаризации]
        K5[Поиск товаров]
    end
    
    subgraph "Менеджер 👔"
        M1[Формирование отчётов]
        M2[Утверждение документов]
        M3[Контроль инвентаризации]
    end
    
    subgraph "Администратор 🛠️"
        A1[Управление пользователями]
        A2[Настройка системы]
        A3[Резервное копирование]
    end
    
    subgraph "Аудитор 🔍"
        U1[Аудит операций]
        U2[Просмотр истории]
        U3[Формирование отчётов аудита]
    end
```

### 1.3. Детальная диаграмма Use Case для Кладовщика

```mermaid
graph TD
    Кладовщик[👤 Кладовщик] --> UC1[Приёмка товара]
    Кладовщик --> UC2[Отпуск товара]
    Кладовщик --> UC3[Перемещение товаров]
    Кладовщик --> UC4[Проведение инвентаризации]
    Кладовщик --> UC5[Поиск товаров]
    
    UC1 --> UC1_1[Сканирование накладной]
    UC1 --> UC1_2[Подтверждение приёмки]
    UC1 --> UC1_3[Размещение на складе]
    
    UC2 --> UC2_1[Создание расходной накладной]
    UC2 --> UC2_2[Подбор товара]
    UC2 --> UC2_3[Подтверждение отпуска]
    
    UC4 --> UC4_1[Выбор зоны инвентаризации]
    UC4 --> UC4_2[Внесение фактических остатков]
    UC4 --> UC4_3[Формирование акта расхождений]
```

## 2. Диаграмма последовательности (Sequence Diagram)

### 2.1. Последовательность приёмки товара

```mermaid
sequenceDiagram
    participant К as Кладовщик
    participant UI as Веб-интерфейс
    participant API as WarehouseService
    participant DB as База данных
    participant S as Сканер
    
    К->>UI: Начало приёмки
    UI->>API: POST /api/receipts
    API->>DB: Проверить поставщика
    DB-->>API: Данные поставщика
    API-->>UI: Форма для приёмки
    
    loop Для каждого товара
        К->>S: Сканировать штрих-код
        S-->>UI: Артикул товара
        UI->>API: GET /api/products/{sku}
        API->>DB: Найти товар
        DB-->>API: Данные товара
        API-->>UI: Информация о товаре
        К->>UI: Ввести количество
    end
    
    К->>UI: Подтвердить приёмку
    UI->>API: PATCH /api/receipts/{id}/complete
    API->>DB: Обновить остатки (Warehouse)
    DB-->>API: Подтверждение
    API->>DB: Обновить статус документа
    DB-->>API: Подтверждение
    API-->>UI: Успешное завершение
    UI-->>К: Приёмка завершена
```

### 2.2. Последовательность инвентаризации

```mermaid
sequenceDiagram
    participant М as Менеджер
    participant UI as Интерфейс
    participant API как InventoryService
    participant DB как База данных
    participant T как ТСД (Терминал)
    
    М->>UI: Создать план инвентаризации
    UI->>API: POST /api/inventory
    API->>DB: Сохранить план
    DB-->>API: ID плана
    
    par Работа кладовщиков
        loop Для каждой зоны
            T->>API: Запрос товаров зоны
            API->>DB: Получить товары зоны
            DB-->>API: Список товаров
            API-->>T: Данные для проверки
            
            loop Для каждого товара
                T->>T: Сканирование штрих-кода
                T->>T: Ввод фактического количества
                T->>API: Отправить результат
                API->>DB: Сохранить результат
            end
        end
    end
    
    API->>API: Автоматическая сверка
    API->>DB: Получить расхождения
    DB-->>API: Данные расхождений
    API-->>UI: Сформировать акт
    UI-->>М: Акт инвентаризации готов
```

## 3. Диаграмма состояний (State Diagram)

### 3.1. Состояния приходной накладной

```mermaid
stateDiagram-v2
    [*] --> Черновик
    Черновик --> На_утверждении : Сохранить
    На_утверждении --> Утверждена : Утвердить
    На_утверждении --> Отклонена : Отклонить
    Утверждена --> В_процессе : Начать приёмку
    В_процессе --> Завершена : Подтвердить приёмку
    В_процессе --> Отменена : Отменить
    Завершена --> [*]
    Отклонена --> [*]
    Отменена --> [*]
```

### 3.2. Состояния товара на складе

```mermaid
stateDiagram-v2
    [*] --> В_наличии
    В_наличии --> Зарезервирован : Резервирование
    Зарезервирован --> Отпущен : Подтверждение отпуска
    Зарезервирован --> В_наличии : Отмена резерва
    В_наличии --> Перемещается : Начало перемещения
    Перемещается --> В_наличии : Завершение перемещения
    В_наличии --> Списано : Списание
    Отпущен --> [*]
    Списано --> [*]
```

## 4. Диаграмма классов (Class Diagram)

### 4.1. Основные классы системы

```mermaid
classDiagram
    class User {
        +int userId
        +string email
        +string passwordHash
        +string firstName
        +string lastName
        +string role
        +authenticate()
        +hasPermission()
    }
    
    class Product {
        +int productId
        +string sku
        +string name
        +Category category
        +Manufacturer manufacturer
        +getCurrentStock()
        +checkAvailability()
    }
    
    class WarehouseItem {
        +int itemId
        +Product product
        +int quantity
        +int reservedQuantity
        +string location
        +reserve()
        +release()
        +move()
    }
    
    class ReceiptDocument {
        +int documentId
        +string documentNumber
        +Date receiptDate
        +List~ReceiptItem~ items
        +addItem()
        +calculateTotal()
        +complete()
    }
    
    class ReceiptItem {
        +int itemId
        +Product product
        +int plannedQuantity
        +int actualQuantity
        +decimal unitPrice
    }
    
    User "1" -- "*" ReceiptDocument : creates
    Product "1" -- "*" WarehouseItem : storedAs
    ReceiptDocument "1" -- "*" ReceiptItem : contains
    Product "1" -- "*" ReceiptItem : includedIn
```

### 4.2. Классы для отчётности

```mermaid
classDiagram
    class ReportService {
        +generateStockReport()
        +generateMovementReport()
        +generateExpiryReport()
        +exportToPDF()
        +exportToExcel()
    }
    
    class StockReport {
        +Date reportDate
        +Map~Product, Integer~ quantities
        +getTotalValue()
        +getLowStockItems()
    }
    
    class MovementReport {
        +DateRange period
        +List~Movement~ movements
        +getTotalIncoming()
        +getTotalOutgoing()
        +getTurnoverRate()
    }
    
    ReportService ..> StockReport : creates
    ReportService ..> MovementReport : creates
```

## 5. Диаграмма активностей (Activity Diagram)

### 5.1. Процесс проведения инвентаризации

```mermaid
flowchart TD
    Start[Начало] --> A[Создать план инвентаризации]
    A --> B{Выбрать метод}
    B --> B1[Полная инвентаризация]
    B --> B2[Выборочная инвентаризация]
    B --> B3[Циклическая инвентаризация]
    
    B1 --> C[Сформировать ведомость для всего склада]
    B2 --> D[Выбрать зоны для проверки]
    B3 --> E[Определить товары по критериям<br/>ABC-анализ, срок годности]
    
    C --> F
    D --> F
    E --> F
    
    F[Распечатать/отправить на ТСД] --> G[Провести подсчёт]
    G --> H[Внести результаты в систему]
    H --> I[Автоматическая сверка]
    I --> J{Есть расхождения?}
    J -->|Да| K[Сформировать акт расхождений]
    J -->|Нет| L[Завершить инвентаризацию]
    K --> M[Утвердить корректировки]
    M --> L
    L --> End[Конец]
```

---

*Документ подготовлен в рамках курсового проекта по дисциплине «Проектирование информационных систем»*
